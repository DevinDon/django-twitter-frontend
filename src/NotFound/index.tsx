import TopBar from './components/index'
import styles from './index.module.css';

const NotFound = () => {
  return (
    <div>
      <h3 className={styles.title}>对不起，该页面不存在！</h3>
    </div>

  )
}

export default NotFound;
